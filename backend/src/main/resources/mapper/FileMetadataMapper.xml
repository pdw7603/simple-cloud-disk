<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.clouddisk.mapper.FileMetadataMapper">

    <!-- 根据用户ID查询文件列表 -->
    <select id="selectByUserId" resultType="com.example.clouddisk.entity.FileMetadata">
        select id, file_name as fileName, file_path as filePath, file_size as fileSize, user_id as userId, upload_time as uploadTime
        from file_metadata
        where user_id = #{userId}
        order by upload_time desc
    </select>

    <!-- 根据文件ID查询文件信息 -->
    <select id="selectByFileId" resultType="com.example.clouddisk.entity.FileMetadata">
        select id, file_name as fileName, file_path as filePath, file_size as fileSize, user_id as userId, upload_time as uploadTime
        from file_metadata
        where id = #{fileId}
    </select>

</mapper>
