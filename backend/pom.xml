<?xml version="1.0" encoding="UTF-8"?>
<!-- Maven 项目根元素：必须存在，所有配置嵌套在其中 -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <!-- 模型版本：固定 4.0.0（Maven 4 规范） -->
    <modelVersion>4.0.0</modelVersion>

    <!-- SpringBoot 父工程：用于统一管理依赖版本，简化配置 -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>2.7.18</version> <!-- 稳定版本，兼容 Hadoop 3.3.6 + JDK 8 -->
        <relativePath/> <!-- 不使用本地相对路径，从仓库下载 -->
    </parent>

    <!-- 项目基础信息：可根据你的项目修改 -->
    <groupId>com.example</groupId> <!-- 包名前缀 -->
    <artifactId>clouddisk-backend</artifactId> <!-- 项目名称 -->
    <version>0.0.1-SNAPSHOT</version> <!-- 版本号 -->
    <name>clouddisk-backend</name> <!-- 项目显示名称 -->
    <description>简易云盘后端项目（文件+Hadoop模块）</description> <!-- 项目描述 -->

    <!-- 依赖配置：所有项目依赖都放在 <dependencies> 节点内 -->
    <dependencies>
        <!-- SpringBoot Web 依赖：实现 RESTful 接口 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- SpringBoot MySQL 驱动依赖：对接 MySQL 数据库 -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- MyBatis-Plus 依赖：简化数据库操作 -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-boot-starter</artifactId>
            <version>3.5.3.1</version>
        </dependency>

        <!-- Lombok 依赖：简化实体类代码（自动生成 getter/setter 等） -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- SpringBoot 测试依赖：可选，用于单元测试 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Hadoop 客户端依赖：核心，实现 HDFS 操作 -->
        <dependency>
            <groupId>org.apache.hadoop</groupId>
            <artifactId>hadoop-common</artifactId>
            <version>3.3.6</version>
        </dependency>
        <dependency>
            <groupId>org.apache.hadoop</groupId>
            <artifactId>hadoop-hdfs</artifactId>
            <version>3.3.6</version>
        </dependency>
        <dependency>
            <groupId>org.apache.hadoop</groupId>
            <artifactId>hadoop-client</artifactId>
            <version>3.3.6</version>
        </dependency>
    </dependencies>

    <!-- 构建配置：用于打包生成可执行 JAR 包 -->
    <build>
        <plugins>
            <!-- SpringBoot 打包插件：必须添加，否则 JAR 包无法直接运行 -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
